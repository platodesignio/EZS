{
  "buildCommand": "prisma generate && (prisma migrate deploy || echo 'skipping migrations') && next build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "regions": ["iad1"]
}

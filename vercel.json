{
  "buildCommand": "export DATABASE_URL=\"${DATABASE_URL:-$POSTGRES_PRISMA_URL}\" && prisma generate && (prisma migrate deploy || true) && next build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "regions": ["iad1"]
}
